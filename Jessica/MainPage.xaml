<Page
    x:Class="Jessica.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Jessica"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView>
            <SplitView.Pane>
                <StackPanel>
                    <Button></Button>
                </StackPanel>
            </SplitView.Pane>
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button FontFamily="Segoe MDL2 Assets" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">&#xE700;</Button>
                    <Grid x:Name="grdTitleBar" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                        <Rectangle Fill="Yellow"/>
                        <TextBlock VerticalAlignment="Center" Margin="5">
                            My Memorizing Cards
                        </TextBlock>
                    </Grid>
                </Grid>
            </StackPanel>
        </SplitView>
    </Grid>
</Page>
